const divide = function() {
  return 2000 / 100;
};

// Arrow function for square (takes one parameter)
const square = num => num * num;

// Arrow function for add (takes two parameters)
const add = (a, b) => a + b;
